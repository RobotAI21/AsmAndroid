<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-feature
        android:name="android.hardware.telephony"
        android:required="false" />

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.CALL_PHONE"/>
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Design.Light.NoActionBar"
        tools:targetApi="31">
        <activity android:name=".LifeSecondActivity"/>
        <activity android:name=".LifeActivity"/>
        <activity android:name=".TestEventActivity"/>
        <activity android:name=".DemoComponentActivity"/>
        <activity android:name=".SharedReferencesActivity"/>
        <activity android:name=".SingUpActivity"/>
        <activity android:name=".MainMenuActivity"/>
        <activity android:name=".budgets.CreateBudgetActivity"/>
        <activity android:name=".budgets.CreateExpenseActivity"/>
        <activity android:name=".EditBudgetActivity"/>
        <activity android:name=".EditExpenseActivity"/>
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>